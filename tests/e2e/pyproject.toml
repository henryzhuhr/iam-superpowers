[project]
name = "iam-e2e-tests"
version = "0.1.0"
description = "End-to-end tests for IAM service"
requires-python = ">=3.11"
dependencies = [
    "pytest>=7.0",
    "requests>=2.28",
]

[tool.pytest.ini_options]
testpaths = ["."]
